import React from 'react';

const Login = () => {
    return (
        <div>
            <h2>Login</h2>
            {/* Implement login form with Supabase auth */}
        </div>
    );
};

export default Login;